#ifndef STRINGS_H
#define STRING_H

#include <vector>
#include <string>

std::vector<std::string> permute(const std::string& str);
bool is_palindrome(const std::string& str);
void print_telephone_words(const std::string& number);

void run_strings();

#endif
